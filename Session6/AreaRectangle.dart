// Function definition matching the required signature
double area(double width, double height) {
  return width * height;
}

void main() {
  double w = 5.0;
  double h = 10.0;
  
  double result = area(w, h);
  print("The area of the rectangle is: $result"); 
}